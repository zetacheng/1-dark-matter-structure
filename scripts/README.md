# Scripts

This directory is reserved for reviewed symbolic, numerical, processing, and reproducibility scripts. Scientific dependencies and implementations will be added only through a specified gate.

Scripts must identify their inputs, outputs, configuration, and applicable conventions. Processing scripts must never modify raw outputs.
